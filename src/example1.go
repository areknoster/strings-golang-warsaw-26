package main

import "fmt"

func main() {
	Überraschung := "Die Variablen können deutsche Namen haben"
	fmt.Println(Überraschung)
	переменная := "У переменных тоже могут быть русские имена!😄"
	fmt.Println(переменная)
}